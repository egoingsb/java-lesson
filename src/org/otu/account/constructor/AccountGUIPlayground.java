package org.otu.account.constructor;

public class AccountGUIPlayground {	
	public static void main(String[] args) {
	        
			// 회계정보를 담고 있는 파일에서 데이터를 읽어옵니다. 
			// 검색 : file read in java  예) https://stackoverflow.com/a/4909018
			
			
			// 스윙로 UI를 구성합니다. 
			// 검색 : java swing sample   예) https://www.javatpoint.com/java-swing
		
			// 여러 줄의 텍스트를 표시할 콘트롤 (textarea), 금액을 입력할 한줄 텍스트 입력 콘트롤 (textfield), 버튼을 UI에 붙입니다. 
			// 검색 : textarea swing, textfield swing, button swing 
		
			// 버튼을 눌렀을 때 아래 코드가 실행되서 출력할 결과를 얻어내세요. 
			// 검색 : swing button addActionListener 예) https://stackoverflow.com/a/284934			
			//Person p1 = new Person(1, "Michael", 0.5);
			//Person p2 = new Person(2, "Calvin", 0.3);
			//Person p3 = new Person(3, "Robin", 0.2);
			//Person[] profitRate = {p1, p2, p3};
			//Accounting acc = new Accounting(Double.parseDouble(money.getText()), profitRate);
			//String output = acc.makeOutput();
			//Print.file(output);
			
			// 저장된 파일을 textarea에 표시해주세요. 
	}
}
